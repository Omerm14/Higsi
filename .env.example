# Core (required — server fails to boot without these)
PIAPI_KEY=
POSTGRES_URL=
BLOB_READ_WRITE_TOKEN=

# Auth (Clerk — see SETUP.md §1)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
ADMIN_CLERK_USER_ID=

# Billing (Stripe — see SETUP.md §2)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# App
NEXT_PUBLIC_APP_URL=
CRON_SECRET=
DAILY_SPEND_CAP_CREDITS=5000
MONTHLY_CAP_USD=1000

# Optional
SENTRY_DSN=
FAL_KEY=

# Eve prompt agent (Claude Haiku via Anthropic API — falls back to templates if unset)
ANTHROPIC_API_KEY=
